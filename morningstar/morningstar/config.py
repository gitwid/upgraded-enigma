"""Version constants. The protocol version describes the capture
methodology (what the four channels mean, what belongs in each); the
schema version describes the storage layout and is advanced by
migrations. Both are stamped onto every stored object."""

APP_VERSION = "0.1.0"
PROTOCOL_VERSION = "0.1"
